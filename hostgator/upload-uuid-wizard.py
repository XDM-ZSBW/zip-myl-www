import ftplib; from pathlib import Path; ftp = ftplib.FTP(); ftp.connect("myl.zip", 21); ftp.login("mylzipftp@myl.zip", "h8r(wrKhdk"); print("[OK] Connected!"); local_path = Path("../src/setup-wizard.html").resolve(); print(f"[UPLOAD] Uploading setup-wizard.html with UUID subdomains from {local_path}"); ftp.storbinary("STOR setup-wizard.html", open(local_path, "rb")); print("[OK] setup-wizard.html uploaded!"); ftp.quit(); print("[SUCCESS] Setup wizard updated with proper UUID subdomains!")
